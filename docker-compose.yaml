version: "3"
services:
  # client:
  #   container_name: client
  #   image: node:12
  #   depends_on:
  #     - server
  #   environment:
  #     NODE_ENV: dev
  #     PORT: 3000
  #     CONTAINER_PATH: ${CONTAINER_PATH}
  #   ports:
  #     - "3000:3000"
  #   command: >
  #     bash -c "
  #     npm install 
  #     && npm start"
  #   working_dir: ${CONTAINER_PATH}
  #   volumes:
  #     - ./client:${CONTAINER_PATH}
  #     - ${CONTAINER_PATH}/node_modules

  # server:
  #   container_name: api
  #   image: node:12
  #   depends_on:
  #     - postgres
  #   environment:
  #     CONTAINER_PATH: ${CONTAINER_PATH}
  #     DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/db
  #     NODE_ENV: dev
  #     PORT: 8080
  #   ports:
  #     - "8080:8080"
  #   command: >
  #     bash -c "
  #     ./start.sh"

  #   working_dir: ${CONTAINER_PATH}
  #   volumes:
  #     - ./server:${CONTAINER_PATH}
  #     - ${CONTAINER_PATH}/node_modules

  postgres:
    image: postgres:10.4
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
